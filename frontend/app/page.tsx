// frontend/app/page.tsx — Pixpilot Pipeline Dashboard (landing page).
// The submission form now lives at /submit.
import { Dashboard } from "@/components/dashboard/Dashboard";

export default function Home() {
  return <Dashboard />;
}
